## 本文件由AI生成

## 这个项目是一个基于Web的音乐播放器，支持音频播放、歌词显示、进度条控制、播放/暂停等功能。项目主要由三个文件组成：`index.html`、`index.css` 和 `index.js`。下面是对每个文件的详细介绍：

### 1. `index.html`
`index.html` 是项目的入口文件，定义了页面的结构和内容。它包含以下主要部分：

- **页面结构**：
  - 页面分为左右两部分：左侧是音乐播放控制区，右侧是歌词显示区。
  - 左侧包含音乐封面、播放进度条、时间显示、播放/暂停按钮等。
  - 右侧是一个滚动区域，用于显示歌词。


- **文件输入**：
  - 通过 `<input type="file">` 元素，用户可以上传音频文件、歌词文件（.lrc）和封面图片。

- **音频播放器**：
  - 使用 `<audio>` 元素来播放音频文件。

- **背景动画**：
  - 使用 `<div class="background">` 元素来创建动态背景效果，背景颜色会根据上传的封面图片自动调整。

### 2. `index.css`
`index.css` 是项目的样式文件，定义了页面的布局、颜色、动画等视觉效果。主要包含以下部分：

- **全局样式**：
  - 设置了全局字体、用户选择行为（禁止用户选择文本）等。

- **布局**：
  - 使用 `flexbox` 布局来排列页面元素，确保页面在不同屏幕尺寸下都能良好显示。

- **音乐封面和播放按钮**：
  - 定义了音乐封面的样式，包括大小、圆角、阴影等。
  - 播放/暂停按钮的样式和动画效果。

- **进度条**：
  - 定义了进度条的样式，包括背景颜色、高度、圆角等。

- **歌词显示**：
  - 定义了歌词的样式，包括字体大小、行高、颜色、透明度等。
  - 使用 `scrollbar-width: none;` 和 `::-webkit-scrollbar` 隐藏滚动条。

- **背景动画**：
  - 使用 `@keyframes` 定义背景动画，通过 `radial-gradient` 创建动态背景效果。

### 3. `index.js`
`index.js` 是项目的脚本文件，负责处理用户交互、音频播放、歌词同步等功能。主要包含以下部分：

- **事件监听**：
  - 监听用户点击音乐封面的事件，触发文件选择对话框。
  - 监听文件选择事件，处理用户上传的音频文件、歌词文件和封面图片。
  - 监听音频播放器的 `timeupdate` 事件，更新播放进度条和时间显示。

- **音频播放控制**：
  - 实现播放/暂停功能，切换播放/暂停按钮的显示状态。

- **歌词解析与同步**：
  - 解析 `.lrc` 歌词文件，将歌词按时间戳分组。
  - 根据当前播放时间，高亮显示对应的歌词行，并自动滚动到当前歌词位置。

- **背景颜色提取**：
  - 从上传的封面图片中提取主色调，并应用到页面背景中。

- **滚动处理**：
  - 监听用户滚动歌词区域的事件，暂停自动滚动功能，允许用户手动滚动查看歌词。

### 项目功能总结
- **音频播放**：支持播放用户上传的音频文件，显示播放进度和时间。
- **歌词同步**：支持上传 `.lrc` 歌词文件，并根据当前播放时间高亮显示对应的歌词行。
- **封面显示**：支持上传封面图片，并根据图片颜色动态调整页面背景。
- **播放控制**：提供播放/暂停按钮，用户可以控制音频的播放状态。
- **进度条控制**：用户可以通过拖动进度条来调整播放进度。
- **响应式设计**：页面布局适应不同屏幕尺寸，确保在移动设备和桌面设备上都能良好显示。

### 项目亮点
- **动态背景**：根据封面图片的颜色生成动态背景，提升视觉效果。
- **歌词同步**：精确同步歌词显示，支持手动滚动查看歌词。
- **用户交互**：通过点击封面选择文件，拖动进度条控制播放进度，提供良好的用户体验。

